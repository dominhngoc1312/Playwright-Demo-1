// Vòng lặp for (i))
// for (<điều kiện khởi tạo>;<điều kiện lặp>;<điều kiện thay đổi>) { //code };

for (let i = 0; i <10; i += 2)
    {
        console.log (i+1 + "hello");
         console.log(i+2 + "hello");
    }

// Khởi tạo: chạy 1 lần duy nhất, ở đầu vòng lăpk
// Điều kiện: chạy ở đầu mỗi vòng lặp, nếu đúng => chạy code trong khối for 
// Thay đổi: chạy ở cuối vòng lặp, sau khi code đã chạy xong

// i++ => i = i+1
// i+=2 => i = i+2
//-------------

// 1 - snake_case: tất cả các chữ viết thường, cách nhau bởi dấu gạch dưới: xin_chao
// 2 - kebab-case: tất cả các chữ viết thường, cách nhau bởi dấu gạch ngang: xin-chao
// 3 - camelCase: chữ đầu viết thường, các chữ sau viết hoa chữ cái đầu tiên: xinChao
// 4 - PascalCase: tất cả chữ cái đầu viết hoa: XinChao 


// kebab-case: đặt tên file và tên folder
// camelCase: đặt tên biến, tên hàm
// PascalCase: đặt tên class